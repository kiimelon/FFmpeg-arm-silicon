# FFmpeg-arm-silicon

Apple Silicon (arm64) FFmpeg build project for macOS.

## Goal

- Build FFmpeg / FFprobe / FFplay
- Target Apple Silicon (arm64)
- Use static dependencies

## Status

Stage 1: initialization
